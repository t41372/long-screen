# Long Screen：懸浮物移除的架構選擇與驗證

## 範圍

審查基準：`962e4eaee7cd6f8eacdf2d15558b4b8d0389c257`；本回合查詢的遠端 main 仍指向此提交。Coding agent 的本地未提交更動不在審查範圍。d/e 的具體瑕疵依使用者貼出的 agent 回報；本回合沒有取得這兩段錄影及其 harness，沒有重新量測其修復率。

這是設計評估與独立小型 PoC，不是已整合的修補版。PoC 是 Python/NumPy/SciPy，使用已知的垂直整數位移、灰階合成影格與簡單 ±2 灰階雜訊；不是 H.264/HEVC、Rust/Wasm 或真機瀏覽器測試。程式沒有量測端到端速度、記憶體與功耗。

## 結論

採用「頁面／螢幕／局部物件運動證據 + 可見性條件化的實際來源選擇 + 衝突區稀疏候選保存」。不要將全畫布按影格數多數決當成最後裁決。

普通區域保留現有單來源瓦片；只有矛盾、遮擋或動態版本區域升級為多候選。像素值一律來自觀測，統計量用於決定來源，不直接作為輸出像素。固定 UI 的 context/presentation 保留一次，不因移除懸浮物而裁掉四周框架。

## 對 agent 評論的修正

1. 根因方向合理，但滑塊與游標不是始終固定螢幕座標；前者在螢幕邊緣以另一速度移動，後者可能自由移動。共同點是不服從該頁面／pane 的運動。
2. 現有核心不是完全沒有跨幀投票。`voting.rs` 已有有限環形緩衝、位移分散的參照幀、三態判斷。`compare` 同時更新新舊影格，直到 eviction/finalize，故舊影格已可能收到未來觀測。純粹宣稱新增 backward/forward 就能解決，證據不足。
3. 投票的 3×3 模糊、較寬鬆灰階容差與鄰域「任何像素接近就同意」適合作為保守線索，不適合成為恢復頁面像素的強正面證據。大面積背景色、細游標、半透明滑塊特別容易混淆。
4. 乾淨觀測佔多數不是必要条件，也不是无条件充分条件。一次可靠乾淨觀測可以勝過很多已判定遮擋；反之，少量對齊錯誤或版本混合能破壞多數決。
5. 區塊投票不能單獨保證時間一致性。一個跨多區塊的元件仍可能拼出從未存在的狀態。必須以動態連通區／時間版本共同約束來源。
6. 首尾幀不等於不可恢復。先區分沒有乾淨觀測、存在乾淨觀測但無法識別、正確識別卻被 compositor 拒用。Issue #2 的一些限制屬後兩者，並非任意方法皆不可解。

## 原始碼定位

- `rust/core/src/chrome.rs::sticky_occlusions`：先比較零位移與頁面位移的紋理，最後仍要求 `stationary_boundary`。邊界要求超過 68% 橫向採樣點相鄰行差異大於 3；與頁面同底色的頂欄沒有這種邊界時，已有固定文字證據也可能無法形成 band。carry 要逐位元相同，對壓縮、hover、文字刷新需另測。
- `rust/core/src/voting.rs::Ring::{partners,compare,finalize}`：比較同時回寫兩幀；灰階容差與局部搜尋是弱正面證據。不能把未被標記當成已證明乾淨。
- `src/pipeline/consistency.ts`：原生 ±1 帧差異會連帶否決乾淨觀測；無可比鄰居預設接受，是保留 glimpse 的重要現有語義。
- `rust/core/src/compositor.rs::composite_block`：`identical == count && provisional_in_block == 0` 提前 return，會先於 `obs.consistent` 的負面證據更新離開；後面的更新 loop 還受 replace/fresh/provisional 條件限制。這與「不一致觀測若重現已儲存像素，也應降級儲存像素」的意圖有落差。應把 evidence/metadata 更新與 pixel-write decision 分離；不能只移除一個 return。

這些是靜態程式路徑判讀；包內 PoC 不執行上述原 Rust 函式。

## 推薦演算法

### A. 先固定座標與狀態模型

對 pane r、影格 t，以最終 canonical canvas 與已校正 pose 定義：`world = screen + pose[r,t]`。不能將 solve 前座標的 candidate keys 直接拿到 pose graph 修正後使用。

至少區分：page-static、page-dynamic、persistent context chrome、temporary screen/local-motion overlay、unknown。固定頁面邊距不是遮擋。Sticky 元件有 page-anchored、pinned、transition 三種時間狀態，不能用整段影片單一全域矩形永久裁掉。

### B. 競爭運動模型產生線索

比較小 patch 在三種對齊下的殘差：

- 頁面模型：`I_t(s)` 對 `I_u(s + pose_t - pose_u)`。
- 螢幕模型：`I_t(s)` 對 `I_u(s)`。
- 局部物件模型：對應游標／滑塊／進出中的 toast 的局部追蹤位移。

螢幕模型明顯較好，且在有資訊量、不同 pose 的觀測上重複成立，才能作為 screen-fixed seed。兩者皆好常是留白或重複紋理；兩者皆差可能是頁面動畫、遮擋、錯誤 pose，不能直接等同滑鼠。

參照幀以有效重疊、資訊量、位置多樣性與遮擋 footprint 選擇；不是只看時間距離，也不是每個物件都套同一個 Dmin。暫停期間不得無限增票。分組需包含遮擋／頁面版本狀態，不能僅依 pose 去掉 hover 消失等重要變化。

### C. 物件與時變遮擋範圍

同背景色頂欄：由固定文字／圖示的運動證據建立候選區，配合內容在此區消失、之後重新露出的證據推估遮擋範圍。可見背景邊界是輔助，不是必要條件。僅有文字 bounding box 不足以涵蓋頂欄底色、陰影或被擦掉的頁面文字；邊界無證據之處維持 unknown。

滑塊：追蹤邊緣窄條中的局部物件與 alpha 污染；不能把整側邊永久裁掉。游標：從不服從頁面模型的局部殘差作短時追蹤，不假定零螢幕位移。URL preview：時間性左下角物件；出現前、消失後可提供 clean witness。

分析尺寸找候選、原生 ROI 精修 mask；細線、陰影、半透明像素不能只靠粗分析格。此物件範圍推估與局部追蹤尚未由本 PoC 整合驗證。

### D. 可見性条件化，而非數人頭

每個世界小區塊保留代表觀測、有效像素 mask、來源 frame/pose、內容 epoch、支持 footprint 與品質。候選相似度只在共同可信可見的部分比較，并要求最低資訊量，不能讓小塊共同留白成為相同版本的充分證據。

先排除被可靠證據判定為 overlay 的像素；unknown 不是 clean，也不是應丟棄。然後比較跨不同 footprint 的支持度，保留少數但可信的 clean witness。暫停重複影格最多有限權重；同一 pose 下物件消失則保留為新狀態證據。

選代表來源或 medoid（最有代表性的實際觀测），不要輸出多幀均值或通道中位數。兩個候選難分時延後裁決並保留低置信標記；不依賴第一筆永久擁有權。

### E. 偵測尺度與輸出來源尺度分開

Mask 可以像素級；候選存取可用 16×16 區塊；輸出應儘量選相鄰 patch 同源，動態元件以更大的連通區／epoch 同源。不要混淆這三種尺度。

靜態頁面可能沒有任何一幀整塊乾淨，但不同幀的乾淨部分聯集覆蓋整塊；這時允許帶來源記錄的局部拼接。動態元件則不能照搬：若不存在同時完整可見的狀態，保持 partial/uncertain，而不是拼出不存在的數字、按鈕或影片瞬間。

### F. 有界工作集、稀疏磁碟資料

普通瓦片只存一份。衝突塊可用 2–4 個候選作初始容量，不是正確性保證；罕見乾淨候選不可只因票少被逐出。溢位應 spill 或明確標 candidateOverflow/ambiguous。

活動 candidates 與 tile 使用 LRU；磁碟依 tile 批量寫入候選 metadata 與必要 native patch。僅存 frame id 會把成本移到重解碼；需要按 frame/GOP 排序重讀，或追加一次順序 replay。不得對每個 patch 隨機 seek 解碼一次 GOP。

100M 像素四份 RGBA 的原始 payload 是 1.6GB，故不能全畫布四份常駐。反過來，10000 個 16×16 衝突塊各額外兩份 RGBA 的纯 payload 是 20.48MB；不是總 RSS，還有 mask、descriptor、metadata、codec 与 cache。

此為資料量推算，非速度／記憶體 benchmark。所有新算子可先沿現有 CPU/Rust/Wasm 路徑實作；不要新增必備 WebGPU，也不要把更多 worker 當成已證實加速。

## 整合位置

1. 保留 scan、layer tracking、pose graph、native tiling、export 架構。
2. `voting.rs` 提供參照選擇與弱 consistency evidence；新增更強的 screen/local-motion witness 與物件時段，不冒用舊 clean bit 作硬證明。
3. `chrome.rs` 將 appearance boundary 從必要門檻改成附加證據，並增加逐像素/時段 mask 途徑。
4. 在最終 pose 與 fragment canonical 化之後，建立 conflict candidate index。未修正座標的 records 必須重投影或重算。
5. `compositor.rs` 分離證據更新、候選評分、像素寫入，保證 metadata-only change 也會 dirty/flush。
6. 將 deferred source resolution 接在 render/temporal conflict 流程；必要時追加一次順序解碼，而非每帧持有全部歷史。
7. provenance/quality metadata 增加 clean witness、screen overlay suspicion、single observation、no clean source、candidate overflow、dynamic partial 等原因。

不保證單靠兩趟正反掃描解決邊界問題：如果沒有新增能辨識哪個觀測遭遮擋的證據，原來的二選一歧義仍在。

## 本回合 PoC

執行 `python run_poc.py`，原始結果見 `results.json`。

### 影像證據，不使用真值 mask 當輸入

每個案例 25 幀，指定 8×4 世界片段在 24 幀被同背景色文字覆蓋，只有一幀乾淨。screen seed 只看影格與已知 pose；選擇實際來源。

| 噪聲    | 額外種子數 | 未加可見性條件的 medoid 選中乾淨来源 | screen-conditioned 選中乾淨来源 | 新版指定片段 MAE |
| ------- | ---------: | -----------------------------------: | ------------------------------: | ---------------: |
| 無      |         20 |                                 2/20 |                           20/20 |                0 |
| ±2 灰階 |         20 |                                 2/20 |                           20/20 |           1.2391 |

限制不能省略：這是固定小片段選擇，不是整張圖清理率。Seed 僅覆蓋約 30.7% 的完整遮擋 footprint、約 60.2% 的有害遮擋像素。兩組案例總共還有 11 個 seed 假陽性；10 組無遮擋控制（合計 3,072,000 影格像素）是 0。這些結果支持將 seed 用作線索，不支持把它當完整可靠 mask。沒有測移動游標、半透明、pose 誤差、真實壓縮與跨平台效能。

### 受控反例／oracle，不是 detector 成績

- 停留偏差：90 個同 footprint 的遮擋觀測 + 4 個不同 footprint 的乾淨觀測。普通 medoid 錯 256/256；按 footprint 平衡後錯 0/256。
- 兩幀的遮擋落在區塊不同角落：完整乾淨 source frame 數 = 0，但 256 像素各自都存在乾淨觀測。使用真值 mask 的來源拷貝可以全對。此例只證明「每塊必須有整幀乾淨候選」的限制不必要。
- 同一動態元件跨三區塊，真實狀態為 110、101、011。各塊獨立多數決生成不存在的 111；元件同源保留實際状态。
- 完全不透明、從未揭露的 16 個像素，可以有兩種不同底圖卻產生完全相同錄影。此時無法由錄影唯一確定真值。

## d/e 的驗收設計

先固定 poses，分離 renderer/overlay 改善與 registration 變化；再做完整 pipeline 測試。

對受損世界像素統計：總觀測數、乾淨觀測數、獨立 footprint 數、同版本 clean witness、是否整個動態元件有共同乾淨時刻。至少分成 never clean、once clean、clean minority、clean majority。只量 clean majority 會低估可修復上限。

「乾淨」不能由待測 detector 自己標記再當真值。合成場景使用獨立 ground truth；實錄需人工核對 mask、來源幀或配對乾淨截圖。同時記錄資料支持／人工確認／無法判定三類，不以最後輸出肉眼順眼取代真值。

消融項：現版；去除純影格數偏差；加 screen/local-motion evidence；加稀疏 candidates/deferred resolution；加 dynamic component coherence。以同一組測試比較實際錯誤與成本。

必看指標：recoverable overlay 殘留、乾淨頁面被誤刪／誤換、missing/invented、時間不一致、來源可追溯性、unknown 正確標示、總執行時間、額外解碼次數、peak resident bytes、磁碟量。不能只把 provisionalPixels 下降當好事。

場景包含：慢滾/快速 fling、只有一幀乾淨、長時間停留、首尾、反向、游標移動/停止、scrollbar alpha/淡出、preview 出現/消失、同底色/無邊界 header、pin/unpin、頁面動畫/計數器、多 pane、壓縮、微小 pose error。Mac/iPhone/Windows/Linux 與目標瀏覽器需後續實機驗收；本包不宣稱已通過。

## 來源

固定提交內：

- `rust/core/src/chrome.rs`
- `rust/core/src/voting.rs`
- `rust/core/src/consistency.rs`
- `src/pipeline/consistency.ts`
- `rust/core/src/compositor.rs`
- `https://github.com/t41372/long-screen/issues/2`

相關方法脈絡：Agarwala 等人的 Interactive Digital Photomontage（SIGGRAPH 2004）把來源選擇與接縫最佳化分開，包含 clean-plate production；這不意味其梯度域混合適合文字截圖，也不證明本文提出架構已完成。
`https://grail.cs.washington.edu/projects/photomontage/`
